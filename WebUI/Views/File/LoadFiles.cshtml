@{
    ViewBag.Title = "LoadFiles";
}

@using (Html.BeginForm("LoadFiles", "File", FormMethod.Post, new { enctype = "multipart/form-data", @class = "css-load-form js-load-form" }))
{
    <fieldset>
        <legend>Load File:</legend>
        <div>
            <label for="file">Select File:</label>
            <input id="file" name="file" type="file" class="" />
        </div>
        <div>
            <label for="description">Description</label>
            <textarea name="description" cols="50"></textarea>
        </div>
        <div>
            <fieldset>
                <legend>Access</legend>
                <div>
                    <select name="isPublic" class="css-file-access">
                        <option value="true">Public</option>
                        <option value="false" selected>Private</option>
                    </select>
                </div>
            </fieldset>
        </div>
        <a class="css-button js-load-button">Load</a>
        <input type="submit" value="Load" class="css-button" style="display: none" />
    </fieldset>
}
@section styles
{
    @Styles.Render("~/Content/themes/base/css")
    @Styles.Render("~/Content/dialog.css")
}
@section Scripts {
    @Scripts.Render("~/Scripts/application/loadFile.js")
    @Scripts.Render("~/bundles/file")
    @Scripts.Render("~/bundles/jqueryval")
}
